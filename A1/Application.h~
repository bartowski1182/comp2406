#ifndef APPLICATION_H
#define APPLICATION_H
using namespace std;
#include <string>
#include "Student.h"
#include "CourseRelatedQueue.h"

class Application{

	public:
		Application (string, int, Student*, CourseRelatedQueue*);
		~Application();
		int AppNum ();
	private:
		int applicationNum;
		string course;
		int status;
		Student* student;
		CourseRelatedQueue* CRHead;/*
		TaCourseQueue* TCHead;
		WorkExpQueue* WEHead;*/
};

#endif
