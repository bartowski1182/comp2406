#include "Util.h"

string Util::charToString(char *c)
{
  string s = c;
  return s;
}

//DYNAMICALLY ALLOCATES MEMORY, REMEMBER TO DEALLOCATE LATER
int Util::stringToChar(string s, char** c)
{
  *c = new char[MAX_BUF];
  std::strcpy(*c, s.c_str());
  return 0;
}
